export enum eRunState {
    stopped,
    starting,
    running
}

export enum eActivityState {
    flashing,
    answering,
    results,
    none
}

export enum eDelayState {
    countdown,
    none
}