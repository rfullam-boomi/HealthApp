export enum eRunState {
    stopped,
    starting,
    running,
    complete
}

export enum eActivityState {
    flashing,
    answering,
    results,
    none
}

export enum eDelayState {
    countdown,
    none
}

export enum eInactivityState {
    watching,
    none
}